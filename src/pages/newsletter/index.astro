---
import Layout from '@/layouts/Layout.astro';
import { getCollection } from 'astro:content';

const title = 'Newsletter: SoundIn | Notes from Scotland';
const description = 'Quiet, irregular notes on building software, hosting events, and running a Scottish technology company. Subscribe or browse the archive.';

const posts = (await getCollection('newsletter')).sort(
  (a, b) => b.data.publishedAt.getTime() - a.data.publishedAt.getTime(),
);

const blogSchema = {
  '@context': 'https://schema.org',
  '@type': 'Blog',
  '@id': 'https://soundin.scot/newsletter#blog',
  name: 'SoundIn Newsletter',
  description,
  url: 'https://soundin.scot/newsletter',
  publisher: { '@id': 'https://soundin.scot/#organization' },
  blogPost: posts.map((p) => ({
    '@type': 'BlogPosting',
    headline: p.data.title,
    description: p.data.summary,
    datePublished: p.data.publishedAt.toISOString(),
    url: `https://soundin.scot/newsletter/${p.id}`,
  })),
};

const dateFmt = new Intl.DateTimeFormat('en-GB', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<Layout
  title={title}
  description={description}
  canonical="https://soundin.scot/newsletter"
  current="newsletter"
  skipLink={{ href: '#newsletter-signup', label: 'Skip to signup' }}
>
  <script is:inline type="application/ld+json" set:html={JSON.stringify(blogSchema)} slot="head" />

  <div class="page-hero">
    <div class="page-hero-inner">
      <span class="page-hero-kicker">newsletter</span>
      <h1 class="page-hero-title">Notes from<br />Scotland</h1>
      <p class="page-hero-desc">
        When a product launches, a market shifts, or something is worth saying, it goes here first. Quiet and irregular.
      </p>
    </div>
  </div>

  <main class="nl-body">
    <section class="nl-signup" id="newsletter-signup" tabindex="-1">
      <h2 class="nl-signup-title">Notes on building the operational record nightlife has been missing.</h2>
      <p class="nl-signup-intro">Occasional dispatches from SoundIn: product progress, the practical work of running nights, and what we are learning with the founding cohort.</p>
      <form id="signup-form" class="signup-form">
        <label for="signup-email" class="sr-only">Email address</label>
        <div class="signup-row">
          <input
            id="signup-email"
            name="email"
            type="email"
            class="signup-input"
            placeholder="you@example.com"
            autocomplete="email"
            spellcheck="false"
            aria-describedby="signup-help signup-status"
            aria-invalid="false"
            required
          />
          <button type="submit" class="cta-button">Subscribe <span aria-hidden="true">→</span></button>
        </div>
        <p id="signup-help" class="signup-help">The notes are occasional. Read our <a href="/privacy">privacy notice</a> before subscribing.</p>
        <p id="signup-status" class="signup-status" role="status" aria-live="polite"></p>
      </form>
    </section>

    {posts.length > 0 && (
      <section class="nl-archive">
        <h2 class="nl-archive-title">Archive</h2>
        <ol class="archive-list" reversed>
          {posts.map((p) => (
            <li class="archive-item">
              <a href={`/newsletter/${p.id}`} class="archive-link">
                <div class="archive-meta">
                  <span class="archive-issue">№ {String(p.data.issue).padStart(2, '0')}</span>
                  <time datetime={p.data.publishedAt.toISOString()} class="archive-date">
                    {dateFmt.format(p.data.publishedAt)}
                  </time>
                </div>
                <div class="archive-text">
                  <h3 class="archive-title">{p.data.title}</h3>
                  <p class="archive-summary">{p.data.summary}</p>
                </div>
                <span class="archive-cta" aria-hidden="true">Read →</span>
              </a>
            </li>
          ))}
        </ol>
      </section>
    )}

    {posts.length === 0 && (
      <section class="nl-empty-archive" aria-labelledby="first-note-title">
        <span>Archive / forthcoming</span>
        <h2 id="first-note-title">The first note is being prepared.</h2>
        <p>Subscribe for the first published issue and product updates from the founding phase.</p>
      </section>
    )}
  </main>
</Layout>

<style>
  .nl-body {
    max-width: 1320px;
    margin: 0 auto;
    padding: 90px clamp(18px, 4vw, 64px) 130px;
    display: grid;
    grid-template-columns: 1.15fr .85fr;
    gap: 0;
  }

  /* Signup */
  .nl-signup {
    margin: 0;
    padding: 0 clamp(36px, 7vw, 110px) 72px 0;
    border-right: 1px solid var(--color-faint);
    border-bottom: 0;
  }

  .nl-signup-title {
    font-family: var(--font-sans);
    font-weight: 560;
    font-style: normal;
    font-size: clamp(40px, 5vw, 72px);
    letter-spacing: -.055em;
    line-height: .98;
    color: var(--color-snow);
    margin-bottom: 32px;
    text-wrap: balance;
  }

  .nl-signup-intro { max-width: 570px; margin: 20px 0 30px; color: var(--color-muted); font-size: 15px; line-height: 1.7; }

  .signup-form {
    max-width: 620px;
  }

  .signup-row {
    display: flex;
    gap: 0;
    align-items: stretch;
  }

  .signup-input {
    flex: 1;
    background: transparent;
    border: 0;
    border-bottom: 1px solid var(--color-faint);
    border-radius: 0;
    padding: 14px 0;
    font-family: var(--font-sans);
    font-size: 18px;
    color: var(--color-snow);
    transition: border-color 0.3s ease, background 0.3s ease;
    min-width: 0;
  }

  .signup-input::placeholder { color: var(--color-muted); }

  .signup-input:focus {
    outline: none;
    border-color: var(--color-salmon);
  }

  .signup-input[aria-invalid="true"] { border-color: var(--color-snow); }
  .signup-row .cta-button { min-width: 132px; justify-content: space-between; }

  .signup-help {
    margin-top: 13px;
    color: var(--color-muted);
    font-size: 12px;
    line-height: 1.5;
  }

  .signup-help a { color: var(--color-cream); text-decoration: underline; text-underline-offset: 3px; }
  .signup-help a:focus-visible { outline: 2px solid var(--color-salmon); outline-offset: 3px; }

  .signup-status {
    font-family: var(--font-sans);
    font-size: 13px;
    color: var(--color-muted);
    min-height: 18px;
    margin-top: 12px;
  }

  .nl-empty-archive { max-width: none; padding: 4px 0 0 clamp(36px, 5vw, 76px); }
  .nl-empty-archive > span { color: var(--color-muted); font-family: var(--font-mono); font-size: 8px; font-weight: 400; letter-spacing: .05em; text-transform: uppercase; }
  .nl-empty-archive h2 { margin-top: 72px; color: var(--color-snow); font-family: var(--font-sans); font-size: clamp(34px, 4vw, 58px); font-style: normal; font-weight: 560; letter-spacing: -.05em; line-height: 1; }
  .nl-empty-archive p { max-width: 500px; margin-top: 18px; color: var(--color-muted); font-size: 14px; line-height: 1.65; }

  /* Archive */
  .nl-archive { padding-left: clamp(36px, 5vw, 76px); }

  .nl-archive-title {
    font-family: var(--font-sans);
    font-weight: 560;
    font-style: normal;
    letter-spacing: -.04em;
    font-size: clamp(24px, 3vw, 36px);
    color: var(--color-snow);
    margin-bottom: 40px;
  }

  .archive-list {
    list-style: none;
  }

  .archive-item {
    border-top: 1px solid var(--color-ghost);
  }

  .archive-link {
    display: flex;
    gap: 24px;
    align-items: baseline;
    padding: 36px 0;
    transition: padding-left 0.3s ease;
  }

  .archive-link:hover { padding-left: 8px; }

  .archive-link:focus-visible {
    outline: 2px solid var(--color-salmon);
    outline-offset: 4px;
    border-radius: 4px;
  }

  .archive-meta {
    flex-shrink: 0;
    width: 140px;
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .archive-issue {
    font-family: var(--font-mono);
    font-weight: 400;
    font-style: normal;
    font-size: 22px;
    color: var(--color-salmon);
  }

  .archive-date {
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-muted);
  }

  .archive-text { flex: 1; }

  .archive-title {
    font-family: var(--font-sans);
    font-weight: 560;
    font-style: normal;
    font-size: clamp(20px, 2.2vw, 28px);
    line-height: 1.2;
    color: var(--color-snow);
    margin-bottom: 6px;
    text-wrap: balance;
  }

  .archive-summary {
    font-family: var(--font-sans);
    font-size: 14px;
    line-height: 1.6;
    color: var(--color-muted);
  }

  .archive-cta {
    font-family: var(--font-sans);
    font-size: 12px;
    font-weight: 700;
    color: var(--color-muted);
    flex-shrink: 0;
    align-self: center;
    transition: color 0.3s ease;
  }

  .archive-link:hover .archive-cta { color: var(--color-salmon); }

  @media (max-width: 900px) {
    .nl-body { padding: 64px 24px 80px; grid-template-columns: 1fr; }
    .nl-signup { padding-right: 0; border-right: 0; border-bottom: 1px solid var(--color-faint); }
    .nl-empty-archive, .nl-archive { padding: 64px 0 0; }
  }

  @media (max-width: 600px) {
    .signup-row { flex-direction: column; }
    .archive-link { flex-wrap: wrap; gap: 12px; }
    .archive-meta { width: auto; flex-direction: row; align-items: baseline; gap: 12px; }
    .archive-cta { display: none; }
  }
</style>

<script>
  const form = document.getElementById('signup-form') as HTMLFormElement | null;
  const input = document.getElementById('signup-email') as HTMLInputElement | null;
  const status = document.getElementById('signup-status');
  const button = form?.querySelector<HTMLButtonElement>('button');

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    if (!input || !status || !button) return;

    if (!input.checkValidity()) {
      status.textContent = "That doesn't look like a valid email address.";
      input.setAttribute('aria-invalid', 'true');
      input.focus();
      input.reportValidity();
      return;
    }

    button.textContent = 'Subscribing…';
    button.disabled = true;
    status.textContent = '';
    input.setAttribute('aria-invalid', 'false');

    try {
      const response = await fetch('/api/newsletter', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ email: input.value }),
      });
      const payload = await response.json().catch(() => ({}));

      if (!response.ok) {
        throw new Error(payload.error || 'Unable to subscribe right now.');
      }

      status.textContent = "You're on the list. We'll be in touch.";
      button.textContent = 'Subscribed';
      input.value = '';
    } catch (error) {
      status.textContent = error instanceof Error ? error.message : 'Unable to subscribe right now.';
      button.textContent = 'Subscribe';
      button.disabled = false;
    }
  });
</script>
