<figure class="signal-object" aria-label="Abstract SoundIn signal instrument">
  <div class="signal-object__meta signal-object__meta--top"><span>S/IN</span><span>FIELD 01</span></div>
  <div class="signal-object__field">
    <span class="signal-object__axis signal-object__axis--x"></span>
    <span class="signal-object__axis signal-object__axis--y"></span>
    <span class="signal-object__ring signal-object__ring--one"></span>
    <span class="signal-object__ring signal-object__ring--two"></span>
    <span class="signal-object__ring signal-object__ring--three"></span>
    <span class="signal-object__core"></span>
    <span class="signal-object__scan"></span>
  </div>
  <div class="signal-object__meta signal-object__meta--bottom"><span>CALM SYSTEMS</span><span>SCOTLAND / 2026</span></div>
</figure>

<style>
  .signal-object {
    position: relative;
    width: min(42vw, 560px);
    aspect-ratio: 1;
    margin: 0;
    padding: 17px;
    display: grid;
    grid-template-rows: auto 1fr auto;
    color: #111;
    background: #dad9d3;
    border: 1px solid rgba(0,0,0,.22);
    overflow: hidden;
  }
  .signal-object::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: linear-gradient(rgba(0,0,0,.08) 1px, transparent 1px), linear-gradient(90deg, rgba(0,0,0,.08) 1px, transparent 1px);
    background-size: 7.143% 7.143%;
    pointer-events: none;
  }
  .signal-object__meta {
    z-index: 2;
    display: flex;
    justify-content: space-between;
    font-family: var(--font-mono);
    font-size: 8px;
    letter-spacing: .04em;
  }
  .signal-object__meta--bottom { align-items: end; }
  .signal-object__field { position: relative; display: grid; place-items: center; }
  .signal-object__axis { position: absolute; z-index: 1; background: rgba(0,0,0,.25); }
  .signal-object__axis--x { width: 100%; height: 1px; }
  .signal-object__axis--y { width: 1px; height: 100%; }
  .signal-object__ring {
    position: absolute;
    z-index: 2;
    aspect-ratio: 1;
    border: 1px solid rgba(0,0,0,.58);
    border-radius: 50%;
  }
  .signal-object__ring--one { width: 76%; }
  .signal-object__ring--two { width: 53%; border-style: dashed; animation: field-rotate 24s linear infinite; }
  .signal-object__ring--three { width: 30%; border-width: 8px; border-color: #111; }
  .signal-object__core {
    z-index: 4;
    width: 9%;
    aspect-ratio: 1;
    border-radius: 50%;
    background: #e9e8e2;
    border: 1px solid #111;
    box-shadow: 0 0 0 10px rgba(233,232,226,.34);
  }
  .signal-object__scan {
    position: absolute;
    z-index: 3;
    width: 38%;
    height: 1px;
    left: 50%;
    top: 50%;
    transform-origin: left center;
    background: #111;
    animation: field-rotate 9s linear infinite;
  }
  .signal-object__scan::after {
    content: '';
    position: absolute;
    right: -4px;
    top: -3px;
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: #111;
  }
  @keyframes field-rotate { to { transform: rotate(360deg); } }
  @media (max-width: 760px) { .signal-object { width: min(78vw, 420px); } }
  @media (prefers-reduced-motion: reduce) {
    .signal-object__ring--two,
    .signal-object__scan { animation: none; }
  }
</style>
