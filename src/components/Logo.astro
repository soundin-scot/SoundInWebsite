---
interface Props {
  ariaLabel?: string;
  href?: string;
}
const { ariaLabel = 'SoundIn: return to homepage', href = '/' } = Astro.props;
---

<a href={href} class="logo" aria-label={ariaLabel}>
  <span class="logo-s">Sound</span><span class="logo-i">In</span>
</a>
